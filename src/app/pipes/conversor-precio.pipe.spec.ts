import { ConversorPrecioPipe } from './conversor-precio.pipe';

describe('ConversorPrecioPipe', () => {
  it('create an instance', () => {
    const pipe = new ConversorPrecioPipe();
    expect(pipe).toBeTruthy();
  });
});
